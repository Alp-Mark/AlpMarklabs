# Railway/Railpack builds from the repo root and installs dependencies before
# the rest of the source is copied, so this file must list deps directly
# (a `-r backend/requirements.txt` reference fails at that build stage).
# Keep this in sync with backend/requirements.txt + worker/requirements.txt.
fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg[binary]
PyJWT
cryptography
bcrypt
reportlab
python-dateutil
itsdangerous
openai

# Worker/Celery dependencies (needed for celery-worker and celery-beat services)
celery
redis
numpy
scipy==1.11.4
scikit-learn==1.3.2
scikit-optimize==0.9.0
ortools==9.15.6755
statsmodels==0.14.4
prophet==1.1.5
boto3==1.34.34
sentry-sdk[celery]==1.40.0
