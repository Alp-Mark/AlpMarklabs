# Railway/Railpack builds from the repo root and installs dependencies before
# the rest of the source is copied, so this file must list deps directly
# (a `-r backend/requirements.txt` reference fails at that build stage).
# Keep this in sync with backend/requirements.txt.
fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg[binary]
PyJWT
cryptography
bcrypt
reportlab
python-dateutil
itsdangerous
openai

# Celery dependencies (needed for celery-worker and celery-beat services)
celery
redis
numpy
boto3
sentry-sdk[celery]

# Optimization Engine dependencies (only loaded when ENABLE_OPTIMIZATION_ENGINE=true)
# Using specific versions known to build on Railway
scipy>=1.11.0,<1.12.0
scikit-learn>=1.3.0,<1.4.0
statsmodels>=0.14.0,<0.15.0
# Note: prophet, scikit-optimize, ortools are optional - engine will work without them
